# Express Hello World

A minimal Express server with a health check route.

## Run

```sh
npm start
```

The server starts at `http://localhost:3000`.

## Routes

- `GET /` returns `Hello, World!`
- `GET /health` returns `{ "status": "ok" }`
